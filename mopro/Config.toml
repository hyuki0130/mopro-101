build_mode = "debug"
target_adapters = ["noir"]
target_platforms = ["react-native"]
react_native = [
    "aarch64-apple-ios-sim",
    "aarch64-apple-ios",
    "aarch64-linux-android",
]
auto_update = false

[update]
